# DSA Spaced Repetition System - Environment Variables

# MongoDB Connection String
# For local development, use the default
# For production, use your own MongoDB instance with strong credentials
MONGODB_URI=mongodb://your-username:your-password@localhost:27017/dsa?authSource=admin

# Alternative for MongoDB Atlas (cloud)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/dsa?retryWrites=true&w=majority

# Development vs Production
NODE_ENV=development